---
name: Bug report
about: Report incorrect behaviour in the attempt binary, an adapter, storage, or a query
title: "bug: "
labels: bug
---

<!--
Do NOT paste prompts, tool output, transcripts, or private paths.
Sanitize logs before attaching them: replace home directories, repository
names other than attemptdb, hostnames, email addresses, and any tokens.
-->

## Environment

- AttemptDB version (`attempt --version`):
- Operating system and architecture (e.g. macOS 15 arm64, Windows 11 x86_64, Ubuntu 24.04 x86_64 glibc/musl):
- Provider and version (e.g. `claude_code` 1.x, `codex` 0.x, `cursor` 1.x, `gemini_cli` 0.x):
- Capture mode (`metadata_only` / `local_semantic` / `full_sync`):
- Data directory location (default or `--data-dir`):

## `attempt doctor` output

<!-- Paste the sanitized output. -->

```text

```

## Steps to reproduce

1.
2.
3.

## Expected behaviour

## Actual behaviour

## Additional context

<!-- Sanitized log excerpts, relevant configuration shape, or a synthetic fixture. -->
